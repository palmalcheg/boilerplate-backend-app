apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/palmalcheg/boilerplate-backend-app:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/palmalcheg/boilerplate-backend-app:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: boilerplate-backend-app
        chartPath: charts/boilerplate-backend-app
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/palmalcheg/boilerplate-backend-app'
          image.tag: '{{.DIGEST_HEX}}'
